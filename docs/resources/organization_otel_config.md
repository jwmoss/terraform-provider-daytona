---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "daytona_organization_otel_config Resource - daytona"
subcategory: ""
description: |-
  Manages OpenTelemetry export configuration for a Daytona organization. Platform-admin / self-hosted only: the managed Daytona cloud rejects this endpoint for an organization owner (HTTP 401); it requires platform-admin credentials or a self-hosted deployment.
---

# daytona_organization_otel_config (Resource)

Manages OpenTelemetry export configuration for a Daytona organization. **Platform-admin / self-hosted only:** the managed Daytona cloud rejects this endpoint for an organization owner (HTTP 401); it requires platform-admin credentials or a self-hosted deployment.

## Example Usage

```terraform
resource "daytona_organization_otel_config" "example" {
  organization_id = "organization-id"
  endpoint        = "https://otel.example.com/v1/traces"

  headers = {
    Authorization = "Bearer token"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `endpoint` (String) OpenTelemetry collector endpoint.
- `organization_id` (String) Daytona organization ID.

### Optional

- `headers` (Map of String, Sensitive) Optional OpenTelemetry request headers.

### Read-Only

- `id` (String) Daytona organization ID for the OpenTelemetry configuration.
